1.项目中如何将所有请求通过zuul转发到各个为服务中的?
答：	使用默认路由，即再配置文件中不做路由配置，发送请求时默认链接为被访问服务的服务名

2.在config加入后测试config是否正常运行，通过链接http://192.168.0.105:8767/foo/dev查看是否正常，这是通过zuul来访问发现
页面报错，后台报错time out ？
答：	在zuul配置文件中加入一下两条属性。
	采用zuul作为网关，根据不同的访问路径进行微服务的路由，譬如有个服务是user，
	我访问user服务的某个接口时，该接口执行时间很慢，2秒多，然后还没执行完，zuul就执行熔断了。
	ribbon.ReadTimeout=60000
	ribbon.ConnectTimeout=60000
	
3.ZuulFilter的使用：顺序，是否都过滤，拦截阶段，run